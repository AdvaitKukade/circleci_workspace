version: 2.1
jobs:
  job_1:
    docker:
      - image: alpine
    steps:
      - run: pwd
      - run: echo "Hello i am groot" >> file1
      - run: echo "Hello i am root" >> file2
      - run: echo "Hello i am tree" >> file3
      - run: echo "Hello i am Leaves" >> file4 
      - run: apk add ca-certificates
      - persist_to_workspace:
          root: ~/
          paths: project/file1

  job_2:
    docker: 
      - image: alpine 
    steps:
      - run: apk add ca-certificates
      - attach_workspace:
          at: /root/home/myfiles

workflows:
  workflows_1:
    jobs:
      - job_1
      - job_2:
          requires:
            - job_1
